import {mutation,query} from "./_generated/server"; import {v} from "convex/values"; import {currentUser} from "./permissions";
export const syncCurrentUser=mutation({args:{name:v.optional(v.string()),email:v.string(),imageUrl:v.optional(v.string())},handler:async(ctx,args)=>{const identity=await ctx.auth.getUserIdentity(); if(!identity) return null; const now=Date.now(); const existing=await ctx.db.query("appUsers").withIndex("by_subject",q=>q.eq("clerkSubject",identity.subject)).unique(); if(existing){await ctx.db.patch(existing._id,{email:args.email.toLowerCase(),name:args.name,imageUrl:args.imageUrl,updatedAt:now}); return existing._id} return await ctx.db.insert("appUsers",{clerkSubject:identity.subject,email:args.email.toLowerCase(),name:args.name,imageUrl:args.imageUrl,createdAt:now,updatedAt:now})}});
export const me=query({args:{},handler:async(ctx)=>{try{return (await currentUser(ctx)).user}catch{return null}}});
