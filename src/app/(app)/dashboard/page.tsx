"use client";
import {useQuery} from "convex/react"; import {api} from "../../../../convex/_generated/api"; import {useCompany} from "@/components/app/company-context"; import {Card} from "@/components/ui/card"; import {Badge} from "@/components/ui/badge";
export default function Dashboard(){const {activeCompanyId}=useCompany(); const data=useQuery(api.analytics.summary,activeCompanyId?{companyId:activeCompanyId}:"skip"); if(!data) return <div className="p-8">Loading dashboard…</div>; const cards=[['Role',data.role],['People in scope',data.scopeSize],['JD tasks',data.jdTaskCount],['One-time tasks',data.oneTimeTaskCount],['Overdue',data.overdueTasks],['Completion rate',data.completionRate+'%'],['Visible SOPs',data.sopCount]]; return <div className="mx-auto max-w-6xl p-8"><div className="mb-6"><div className="text-4xl">📊</div><h1 className="text-[32px] font-bold tracking-[-.6px]">Dashboard</h1><p className="text-[var(--ink-muted)]">Role-aware analytics for your current company context.</p></div><div className="grid grid-cols-2 gap-3 md:grid-cols-4">{cards.map(([k,v])=><Card key={k} className="p-4"><div className="text-xs text-[var(--ink-muted)]">{k}</div><div className="mt-2 text-2xl font-semibold">{v}</div></Card>)}</div><section className="mt-8"><h2 className="mb-3 text-lg font-semibold">Recent activity</h2><Card><table className="notion-table"><tbody>{data.recent.map((e:any)=><tr key={e._id}><td className="px-3"><Badge>{e.action}</Badge></td><td>{e.targetType}</td><td className="text-[var(--ink-muted)]">{new Date(e.createdAt).toLocaleString()}</td></tr>)}</tbody></table></Card></section></div>}
