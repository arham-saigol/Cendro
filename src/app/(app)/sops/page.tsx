"use client";
import Link from "next/link"; import {useMutation,useQuery} from "convex/react"; import {api} from "../../../../convex/_generated/api"; import {useCompany} from "@/components/app/company-context"; import {Card} from "@/components/ui/card"; import {Button} from "@/components/ui/button"; import {Input} from "@/components/ui/input"; import {Textarea} from "@/components/ui/textarea"; import {Badge} from "@/components/ui/badge"; import {useState} from "react";
export default function Sops(){const {activeCompanyId}=useCompany(); const sops=useQuery(api.sops.list,activeCompanyId?{companyId:activeCompanyId}:"skip"); const create=useMutation(api.sops.create); const [title,setTitle]=useState(""); const [content,setContent]=useState(""); return <div className="p-8"><div className="mb-6"><div className="text-4xl">📄</div><h1 className="text-[32px] font-bold">SOPs</h1><p className="text-[var(--ink-muted)]">Searchable operating procedures with company, branch, department, and user visibility.</p></div><Card className="mb-4 p-3"><div className="grid gap-2"><Input value={title} onChange={e=>setTitle(e.target.value)} placeholder="SOP title"/><Textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="SOP body"/><Button variant="primary" onClick={async()=>{if(activeCompanyId&&title.trim()) {await create({companyId:activeCompanyId,title,content,scopeType:"company",branchIds:[],departmentIds:[],userMembershipIds:[]}); setTitle(""); setContent("")}}}>Create SOP</Button></div></Card><Card><table className="notion-table"><tbody>{sops?.map((s:any)=><tr key={s._id}><td className="px-3 font-medium"><Link href={`/sops/${s._id}`}>{s.title}</Link></td><td><Badge>{s.scopeType}</Badge></td><td className="text-[var(--ink-muted)]">Updated {new Date(s.updatedAt).toLocaleDateString()}</td></tr>)}</tbody></table></Card></div>}
